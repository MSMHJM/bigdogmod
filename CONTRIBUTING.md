# 贡献指南

感谢你愿意为 Big Dog Mod 贡献代码！

## 项目结构

本仓库包含 3 个相互独立的版本目录，每个都是完整的 Fabric Loom 工程：

- `26.3-pre-2/`：主版本（预览版）
- `26.1.2/`、`26.2/`：稳定版本移植

## 提交规范

- 提交信息使用 `type: 描述` 格式，例如 `fix: 修复音波多次命中`、`feat: 新增 XX 升级`、`docs: 更新 README`
- 一次提交只做一件事

## 本地构建

每个版本目录独立构建：

```bash
cd 26.2
./gradlew build
```

注意：

- `libs/` 需要放入对应版本的 Fabric API jar 才能构建
- 构建产物在 `build/libs/`
- 改动涉及多个版本时，请分别在对应目录中修改并构建验证

## 测试

- 修改后请在游戏内（或服务端）实测相关功能
- 涉及网络同步（技能、冷却、设置）的改动请重点测试单机暂停与多人环境

## 提交流程

1. Fork 本仓库并创建特性分支
2. 在对应版本目录中修改代码
3. 构建并通过测试
4. 提交 Pull Request，说明改动内容与测试情况
